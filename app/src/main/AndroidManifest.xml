<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissões essenciais -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permissões para auto-update: download e instalação de APK -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".CalculadoraHHApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalculadoraHH">

        <!-- HomeActivity como tela inicial -->
        <activity
            android:name=".ui.activities.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity com as abas -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="false"
            />

        <!-- Histórico de RDOs -->
        <activity
            android:name=".ui.activities.HistoricoRDOActivity"
            android:label="Histórico de RDOs"
            android:parentActivityName=".ui.activities.HomeActivity"/>

        <activity
            android:name=".ui.activities.CalendarioRDOActivity"
            android:label="Calendário de RDOs"
            android:theme="@style/Theme.CalculadoraHH"
            android:exported="false"/>

        <!-- FileProvider para compartilhar arquivos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
